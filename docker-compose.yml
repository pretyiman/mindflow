services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mindmup
      POSTGRES_PASSWORD: mindmup
      POSTGRES_DB: mindmup
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mindmup"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
